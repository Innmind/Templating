arguments:
    templates: Innmind\Url\PathInterface
    cache: '?Innmind\Url\PathInterface'
    helpers: '?map<string, object>'

expose:
    engine: $twig

services:
    twig Innmind\Templating\Twig:
        - $templates
        - $cache
        - $helpers
